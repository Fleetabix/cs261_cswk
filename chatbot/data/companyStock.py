class CompanyStock:
	def __init__(self, spotprice, change, percentchange, retrieve_datetime)
		self.spot_price = spotprice
		self.last_change = change
		self.last_percent_change = percentchange
		self.retrieved = retrieve_datetime